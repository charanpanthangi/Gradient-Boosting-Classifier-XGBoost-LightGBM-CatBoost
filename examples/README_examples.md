# Examples

This folder can hold additional scripts or datasets. For now, the main usage is
running `python app/main.py` to train and evaluate the three boosting models on
the breast cancer dataset. You can add your own datasets or experiment with
hyperparameters here.
